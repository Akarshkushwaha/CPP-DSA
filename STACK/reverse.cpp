#include<iostream>
#include<stack>
using namespace std;

int main(){
  stack<int< s;
  string name = "akarsh";

  //insertion of charecters into the stack

  for(int i=0; i<name.length(); i++){
    char ch = name[i];
    s.push(ch);
  }
  
}